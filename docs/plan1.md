# 「手紙ポスト」アプリ Firebase移行・新機能・交換日記UI/UX 統合仕様書（改訂版）

本ドキュメントは、「手紙ポスト」アプリにおけるローカルモック環境からFirebaseプラットフォームへの移行設計、情緒的なUI/UX演出、新設される「フレンドタブ」「交換日記リレー機能」に加え、**オリジナルフォントの最適化・作成フローの高度化、文字データ通信制限、およびFirebaseのセキュリティ・コスト最適化設計**を網羅した、実装・開発用の最終統合仕様書です。

---

## 1. アプリケーション概要と基本仕様

「手紙ポスト」は、即時性を重視する現代のチャットアプリとは対照的に、あえて手紙が届くまでに時間を要する（配送時間を要する）「スローライフ」なコミュニケーションを提供するSNSです。
デジタルでありながら、本物の紙やペン、ノートを使ったような温かみ、おしゃれな質感、そして届くまでのワクワク感を演出する情緒的なUI/UXを最大の特徴とします。

### 1.1 主要機能と画面構成・情緒的演出仕様
下部ナビゲーションには以下の5つのタブを配置します。中央の「手紙作成」ボタンは丸く大きく際立たせたモダンなUIとし、タップ時の挙動に特殊な画面遷移を導入します。
* **配置**: `ポスト` / `レーダー` / `手紙作成`（中央・丸く大きく配置） / `フレンド` / `マイページ（設定含む）`

#### ① ポスト（受信箱・送信履歴・配送ステータス表示・交換日記閲覧）
* **情緒的な配送ステータス（「てがみをまつ」演出）**: 
    * 配送中の手紙・交換日記は、無機質な秒単位のカウントダウンではなく、「約h時間後」のように大まかに表現します。
    * 画面上には、可愛い配達員や車のイラストとともに、現在の状況が「向かい中…もう少し！」「しわけ中 まだまだ」といった情緒的な言葉で可視化され、手紙を待つ時間を楽しく演出します。
* **「うけとる」・開封演出**:
    * 手紙や交換日記が届くと「手紙がとどきました！」という通知とともに、画面上に味のある「ポスト」が表示されます。
    * 届いたアイテムは「あける」または「すてる」を選択できます。「あける」を選択すると、実際に封筒から手紙を取り出したり、日記帳の表紙を開いたりするような、おしゃれな開封アニメーション演出が入ります。
* **受信アイテムの描画と過去の筆跡維持**:
    * 送信者が指定した便箋/日記テーマ、文字サイズ、文字色、フォント（既定、または手書きオリジナル）で美しく描画されます。
    * 送信者が手紙を送信した「その時点の筆跡」を正確に維持するため、データには送信時のフォントの**バージョン情報（リビジョン番号）**を記録し、送信後にフォントが更新されても過去の筆跡が勝手に書き換わらないように保証します。
* **交換日記のリレーハブ機能**:
    * 自分の順番が回ってきた交換日記がポストに届いた際、これまでの全員の記録を「本（見開きノート）のような質感」でパラパラとページをめくって閲覧できる特別なブックビューアUIを提供します。
    * ポスト内の交換日記詳細画面に「**次の人へ書いて送信**」ボタンを配置し、タップするとシームレスに手紙作成と同じフロー（全画面エディタ）へと遷移します。
    * 「秘密の話」や「グッドニュース」「バッドニュース」「質問相談コーナー」「恋バナ」など、一周回るごとに話題のテーマ（お題）がシステムから与えられます。

#### ② レーダー（置き手紙レーダー）
* フレンドまたは所属グループのメンバーが、特定のGPS位置に設置した「置き手紙（Drop Letter）」を探すための機能です。距離（50m以内）と方向に基づいてレーダー風の画面上にプロットし、実際にその場所まで近づくことで発見・開封できます。

#### ③ 手紙作成 / 交換日記執筆（イマーシブ・全画面エディタ）
* **没入型（イマーシブ）UI**:
    * 下部ナビゲーション中央の丸く大きな「手紙作成」ボタン、またはポストの「次の人へ書いて送信」ボタンを押した瞬間、**下部ナビゲーションバーを完全に隠し、画面全体を執筆専用のエディタ画面へと切り替えます**。これにより、ユーザーが手紙の執筆に完全に没頭できる空間を演出します。
* **軽量な文字データ通信（画像化の禁止）**:
    * 通信コストおよびデータベース容量を最小限に抑えるため、手紙データはすべて**純粋な「文字（テキスト）データ」および「スタイル・位置のメタデータJSON」として送信**します。手紙の背景（便箋デザイン）は、アプリ（クライアント側）にあらかじめアセットとして内蔵されているもの定義IDで指定して呼び出す方式とし、画面の画像化（ラスタライズデータの送信）は行いません。
    * **画像添付制限**: 手紙1通（または交換日記1エントリ）につき、写真データを**1枚のみ**添付可能です（Firebase Storageへアップロード）。
* **おしゃれな執筆・封緘プロセス**:
    1. **「かく（紙選び）」**: 本物の紙の束を「ベラベラ（パラパラ）」とめくって選ぶようなリッチな触感・視覚エフェクトUIで、便箋や日記帳のデザインを決定します。
    2. **「かき中（エディタ・部分フォント適用）」**: 文字数制限はなく、画面上部のツールバーで「ペン（フォント）」「色」「文字サイズ」を**文字・ブロック単位で柔軟に変更しながら**長文を執筆できます。これにより、「手紙の一部だけを自分のオリジナルフォント（手書き）にし、残りは既定の明朝体にする」といった高度な表現が可能です。
    3. **「送信前の不足文字その場作成ステップ（重要）」**: 執筆完了後、手紙（または日記）の中に、ユーザーが**「まだオリジナルフォントに登録していない未作成の文字」が含まれている場合、自動的にシステムが検出**します。次の封緘演出に進む前に、「足りない文字をその場で書く」ポップアップまたはステップ画面を挟み、フォントデータを補完・更新してから送信フェーズへ進みます。
    4. **「かきおわり・しまう演出（封緘）」**: 執筆および文字補完の完了後、ふうとうやシールを選び、便箋が折りたたまれて封筒にシュッと収まる（日記の場合はパタンと閉じられる）アニメーションが走ります。
    5. **「おくる（配送選択）」**: 郵送（距離に応じた所要時間、週3回までの速達制限あり）または置き手紙モードを選択し、最後に手紙をポストに投函する、あるいは相手に直接手渡すような温かみのあるアニメーションを経て送信されます。

#### ④ フレンド（フレンド・グループ管理）
従来のフレンド一覧と、新設されるグループ管理機能を統合したコミュニケーションハブ画面です。
* **フレンド管理**: 「フレンドID検索」「QRコードスキャン」「近距離通信（BLE等を用いた近接端末マッチング）」によるフレンド申請・承認・一覧表示。
* **グループ作成（招待制）**: 
    * 任意のフレンドを複数人選択して、招待制の「グループ」を作成できます。
    * グループ宛てには「メンバー全員への一斉送信手紙」や「グループメンバーなら誰でも開けられる置き手紙（誰かが開封しても、他のメンバーが未開封であればデータは維持）」の作成・設置が可能になります。
* **交換日記の同時生成機能**:
    * グループ作成時に「**交換日記も作成する**」チェックボタンを提供します。
    * これを有効にすると、グループ作成と同時に以下の設定画面へ遷移します。
        * **交換順番の設定**: メンバーの一覧をドラッグ＆ドロップで並び替え、日記を回すローテーション順序（例: A → B → C → A...）を決定します。
        * **交換日記のデザイン選択**: 日記の表紙やベースとなるノートのテーマデザインを選択します。
    * 設定完了後、最初の執筆者が「手紙作成」と同様の流れで第1ページ目を執筆し、送信することで交換日記リレーがスタートします。

#### ⑤ マイページ（旧「設定」統合型プロフィール）
* **アカウント・プロフィール編集**: 自身の表示名、アイコン画像、およびフレンドコード（3〜15文字以上、重複不可バリデーションあり）の管理。
* **手書きオリジナルフォント作成機能（よく使う順作成UI）**:
    * ユーザーはマイページからいつでもオリジナルフォントの作成・追加登録を行うことができます。
    * **作成順序の最適化**: ユーザーが迷わず効率的にフォントを成長させられるよう、フォント作成の提示文字は、利用頻度の高いカテゴリ順（**ひらがな → よく使う記号 → カタカナ → アルファベット → 常用漢字 → その他**）で1文字ずつステップアップしていく構造とします。
    * **フォント作成UI**: 画面上部または中央に「次に書く対象の文字（お手本）」を1文字大きく表示し、その下部に配置された**「大きな正方形（四角形）の手書きキャンバス入力欄」**の中に指やスタイラスペンで直接文字を肉筆で書き込むスタイルとします。
* **アプリ設定項目**: カラーモード（ライト/ダーク/システム同期）、多言語切り替え（日本語/英語など）の設定項目をこの画面内に集約します。

---

## 2. Firebase アーキテクチャ設計＆セキュリティ・コスト最適化設計

### 2.1 Cloud Firestore (データベース設計)


```

/users/{uid} (Document)
├── displayName: String
├── friendId: String
├── photoUrl: String?
├── lastLocation: GeoPoint
├── settings: Map (theme: String, language: String)
├── weeklyExpressCount: Integer
├── lastExpressResetAt: Timestamp
├── fontSettings: Map (hasCustomFont: Boolean, currentFontVersion: Integer)
└── createdAt: Timestamp

/groups/{groupId} (Document)
├── name: String
├── ownerId: String
├── memberIds: Array [uid1, uid2, ...]
├── hasActiveDiary: Boolean
├── diaryId: String?
└── createdAt: Timestamp

/friend_requests/{requestId} (Document)
├── senderId: String
├── recipientId: String
├── status: String (pending, accepted, rejected)
└── createdAt: Timestamp

/friends/{friendshipId} (Document)
├── userIds: Array [uid1, uid2]
└── establishedAt: Timestamp

/letters/{letterId} (Document)
├── senderId: String
├── senderName: String
├── recipientId: String?
├── recipientGroupId: String?
├── isGroupDrop: Boolean
├── openedUserIds: Array [uid1, uid2, ...]
├── mode: String (express, normal, drop)
├── pages: Array [
{
content: String,
paperTheme: String,
textStyles: Array [
{ start: Integer, end: Integer, fontType: String, fontVersion: Integer?, fontSize: Double, color: String }
] // 文字・ブロック単位の部分フォント・スタイル適用メタデータ
}
]
├── photoUrl: String?
├── envelopeTheme: String
├── sealTheme: String
├── sentAt: Timestamp
├── estimatedDeliveryAt: Timestamp
├── geoLatitude: Double?
├── geoLongitude: Double?
├── status: String (transit, delivered)
└── deliveryDurationSeconds: Integer

/diaries/{diaryId} (Document)
├── groupId: String
├── title: String
├── designTheme: String
├── rotationOrder: Array [uid1, uid2, uid3, ...]
├── currentTurnIndex: Integer
├── currentTurnUserId: String
├── status: String (reading, writing, transit)
├── estimatedDeliveryAt: Timestamp?
└── updatedAt: Timestamp

/diaries/{diaryId}/entries/{entryId} (Subcollection)
├── writerId: String
├── writerName: String
├── pages: Array [
{
content: String,
diaryPageTheme: String,
textStyles: Array [ { start: Integer, end: Integer, fontType: String, fontVersion: Integer?, fontSize: Double, color: String } ]
}
]
├── photoUrl: String?
├── sentAt: Timestamp
└── sequenceNumber: Integer

/custom_fonts/{uid} (Document)
├── currentVersion: Integer
├── fontVersions: Map { "1": "https://...", "2": "https://..." } // 必要なデータ（サブセット、またはストロークJSON）のみを格納した軽量ファイルのURL
└── updatedAt: Timestamp

/custom_fonts/{uid}/glyphs/{character} (Subcollection)
├── character: String
├── vectorData: String // 手書きのベクターパスデータ（SVG形式）
├── versionAdded: Integer
└── updatedAt: Timestamp

```

### 2.2 フォントデータのストレージ＆通信量最適化設計
フォントデータによる通信量とサーバーコストを徹底的に削減するため、以下の「手紙専用の最小データ転送」仕様を確定適用します。

* **サブセットフォント/ストローク差分同期の確定採用**:
  * ユーザーのオリジナルフォントを読み込む際、数MBに及ぶ日本語の全文字入りTTF/OTFファイルを丸ごと転送することは一切禁止します。
  * **実装アプローチ**: サーバー（Firestore）上には文字単位の軽量ベクターデータ（`vectorData`: 数百バイトのSVG形式）のみを保管。手紙送信時に、アプリ側またはFunctionsで**「その手紙に実際に使用されている文字データのみ」を抽出・パッケージングした極小のサブセットファイル**、あるいは文字対応のストロークJSONデータのみを生成して転送します。
  * これにより、フォント関連の通信・ファイルサイズは手紙1通あたりわずか数KB〜数十KBへと極小化されます。

### 2.3 セキュリティ・悪用防止・コスト最適化設計
Firebaseの不正利用（DDos、スパム、悪意ある書き込み）を防止し、インフラコストが跳ね上がらないよう、以下の設計ルールをセキュリティルールおよびクライアントロジックに強制適用します。

#### ① Firestore セキュリティルール（Security Rules）の徹底
* **Userコレクション**: `request.auth.uid == uid` の場合のみ書き込み可能（他人のプロフィールの改ざん禁止）。
* **Lettersコレクション**: 
  * `create`: 送信者（`senderId`）が自分自身であること、かつ1回のリクエストサイズや週次速達制限回数が正しいことを検証。
  * `read`: `request.auth.uid == resource.data.senderId` または `request.auth.uid == resource.data.recipientId` または `request.auth.uid in get(/databases/$(database)/documents/groups/$(resource.data.recipientGroupId)).data.memberIds` の場合のみ許可（無関係な第三者による手紙の盗聴を100%遮断）。
* **Diariesコレクション**: `request.auth.uid in resource.data.rotationOrder` の所属メンバーのみ読み書きを許可。

#### ② Firebase Storage セキュリティルールと容量制限
* **画像アップロード制限**:
  * パスを `/letters/{letterId}/attached_photo.jpg` または `/diaries/{diaryId}/{entryId}.jpg` に厳密に固定。
  * ルール側で `request.resource.size < 5 * 1024 * 1024`（最大5MB以下）および `request.resource.contentType.matches('image/.*')`（画像ファイルのみ）のバリデーションを強制し、大容量ファイルや無関係なスクリプトファイルのアップロードによるストレージコスト逼迫を防止。

#### ③ Firebase App Check の導入
* デバイスの整合性（iOS: DeviceCheck/App Attest, Android: Play Integrity）を検証する **Firebase App Check** を有効化。Curlコマンドや外部スクリプト、偽装されたAPIリクエストによるFirestore/Storageへの不正アクセスや悪意ある大量リクエスト（リソース浪費）をSDKレイヤーで完全にブロックします。

#### ④ コスト削減のためのクエリ・キャッシュ戦略
* クライアント側（Flutter）でFirestoreの **Local Cache（永続性ディスクキャッシュ）** を有効化。ポスト内の過去の手紙や交換日記の一覧、フレンド一覧などは、一度取得したデータはキャッシュから読み込み、ドキュメントの変更差分（`snapshots()`）のみをリッスンすることで、FirestoreのRead（読み込み）リクエスト回数とコストを劇的に削減します。
* レーダー機能（置き手紙検索）においては、全件取得を絶対に避け、Geohash（近傍検索クエリ）を用いて自身の現在地周辺（50m~数km圏内）のドキュメントのみを限定的にフックする設計とします。

---

## 3. 開発・移行タスク一覧（TODOリスト）

### フェーズ 1 ~ 2: 基盤構築・アカウント・フレンド
- [ ] Firebaseプロジェクト初期化、匿名認証の実装
- [ ] 近距離通信(BLE)・QR・ID検索によるフレンド追加機能の実装
- [ ] **Firebase App Check** の導入とiOS/Androidのキー/プロバイダ設定（悪用防止策）

### フェーズ 3: ナビゲーション再構築と全画面エディタの実装
- [ ] **ナビゲーション構造の刷新**: `ポスト` / `レーダー` / `手紙作成` / `フレンド` / `マイページ` の5タブ構成
- [ ] **全画面イマーシブUI**: 手紙作成ボタン押下時にナビゲーションバーを非表示化する処理の実装
- [ ] **文字データエディタと部分フォント適用ロジックの実装**:
  - [ ] 画像化せず、純粋なテキスト＋リッチテキストメタデータ（`textStyles`）でFirestoreに保存する仕組みの構築
  - [ ] 文字・ブロック単位でオリジナルフォントと既定フォントを混在して適用できるエディタUIの開発
  - [ ] 添付画像を1枚のみに制限し、5MB以下に圧縮してStorageにアップロードするロジックの実装

### フェーズ 4 ~ 5: グループ・交換日記生成および拡張機能の実装
- [ ] **フレンドタブの構築**: フレンド一覧、招待制グループ作成UIの実装
- [ ] **交換日記同時生成処理**: ドラッグ＆ドロップによる順番設定、テーマ選択、および初回執筆フロー連携
- [ ] **グループ拡張機能**: `recipientGroupId` を用いたメンバー全員への一斉送信および、複数メンバーで既読管理可能なグループ置き手紙（レーダー連携）の実装

### フェーズ 6: 交換日記リレーシステムと「不足文字補完」の実装
- [ ] **送信前不足文字検出＆その場作成ステップの開発**:
  - [ ] 手紙作成完了時（封緘前）、本文の文字をスキャンし、`custom_fonts/{uid}/glyphs` に存在しない文字を抽出するロジックの実装
  - [ ] 未登録文字がある場合、画面中央に「お手本文字」を提示し、「大きな正方形の手書きキャンバス」でその場で肉筆入力させてFirestoreへ追加・フォントバージョンをインクリメントするインターフェースの構築
- [ ] **本・ノート風ビューア**: ポストから交換日記を開いた際、過去エントリを本のようにスワイプしてめくれるUIの実装
- [ ] **リレー連携**: 「次の人へ書いて送信」ボタンから全画面エディタを起動し、ターンインデックスを更新して次のメンバーへ配送するロジックの実装

### フェーズ 7: マイページオリジナルフォント作成機能の高度化
- [ ] **よく使う順作成ステップUIの実装**:
  - [ ] マイページのフォント作成画面において、ひらがな → 記号 → カタカナ → アルファベット → 常用漢字 → その他の順序で文字を提示する制御ロジックの実装
  - [ ] 「大きな四角形（1:1）の手書きキャンバス」を中心とした、洗練されたフォントライティングUIの構築

### フェーズ 8: セキュリティ強化・コスト最適化・テスト
- [ ] **Firestore / Storage Security Rules の厳格な記述とデプロイ**（他人の手紙閲覧禁止、容量制限等）
- [ ] **クエリのローカルキャッシュ有効化設定**と読み込み数抑制のテスト
- [ ] Firebase Emulator Suiteを用いた、セキュリティルールのバリデーションテスト、および交換日記リレーの統合結合テスト