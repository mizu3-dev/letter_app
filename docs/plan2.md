# 「手紙ポスト」アプリ Flutter×Firebase 移行・新機能・交換日記UI/UX 統合仕様書（完全版）

本ドキュメントは、「手紙ポスト」アプリにおけるローカルモック環境からFirebaseプラットフォームへの移行設計、ワイヤーフレームに基づく情緒的なUI/UX演出、新設される「フレンドタブ」「交換日記リレー機能」に加え、**オリジナルフォントの最適化・作成フローの高度化、文字データ通信制限、Firebaseのセキュリティ・コスト最適化設計、手紙作成画面のUI詳細、およびエッジケース対策・グループ送信機能拡張（1人限定受信）**のすべてを内包した、実装・開発用の最終統合仕様書です。

---

## 1. アプリケーション概要と基本仕様

「手紙ポスト」は、即時性を重視する現代のチャットアプリとは対照的に、あえて手紙が届くまでに時間を要する（配送時間を要する）「スローライフ」なコミュニケーションを提供するSNSです。
デジタルでありながら、本物の紙やペン、ノートを使ったような温かみ、おしゃれな質感、および届くまでのワクワク感を演出する情緒的なUI/UXを最大の特徴とします。ただ、ユーザーの操作体験を失わないようメジャーなSNSのデザイン設計も参考にしつつ、アプリ全体のフォントへはゴシック体を用います。

### 1.2 主要開発言語・技術スタック
* **クライアントサイド**: **Flutter (Dart)** を主軸として開発します。マルチプラットフォーム（iOS / Android）において、本仕様書で定義するリッチな全画面アニメーションや、文字ごとの動的スタイル描画、手書きキャンバスのストローク制御をスムーズに実現します。
* **バックエンド・インフラ**: **Firebase** (Authentication, Cloud Firestore, Firebase Storage, Firebase Messaging, Cloud Functions, Cloud Tasks) を全面的に採用します。

### 1.3 主要機能と画面構成・情緒的演出仕様
下部ナビゲーションには以下の5つのタブを配置します。中央の「手紙作成」ボタンは丸く大きく際立たせたモダンなUIとし、タップ時の挙動に特殊な画面遷移を導入します。
* **配置**: `ポスト` / `レーダー` / `手紙作成`（中央・丸く大きく配置） / `フレンド` / `マイページ（設定含む）`

#### ① ポスト（受信箱・送信履歴・配送ステータス表示・交換日記閲覧）
* **情緒的な配送ステータス（「てがみをまつ」演出）**: 
    * 配送中の手紙・交換日記は、無機質な秒単位のカウントダウンではなく、「約h時間後」のように大まかに表現します。
    * 画面上には、可愛い配達員や車のイラストとともに、現在の状況が「向かい中…もう少し！」「しわけ中 まだまだ」といった情緒的な言葉で可視化され、手紙を待つ時間を楽しく演出します。
* **「うけとる」・開封演出**:
    * 手紙や交換日記が届くと「手紙がとどきました！」という通知とともに、画面上に味のある「ポスト」が表示されます。
    * 届いたアイテムは「あける」または「すてる」を選択できます。「あける」を選択すると、実際に封筒から手紙を取り出したり、日記帳の表紙を開いたりするような、おしゃれな開封アニメーション演出が入ります。
* **受信アイテムの描画と過去の筆跡維持**:
    * 送信者が指定した便箋/日記テーマ、文字サイズ、文字色、フォント（既定、または手書きオリジナル）で美しく描画されます。
    * 送信者が手紙を送信した「その時点の筆跡」を正確に維持するため、データには送信時のフォントの**バージョン情報（リビジョン番号）**を記録し、送信後にフォントが更新されても過去の筆跡が勝手に書き換わらないように保証します。
* **交換日記のリレーハブ機能**:
    * 自分の順番が回ってきた交換日記がポストに届いた際、これまでの全員の記録を「本（見開きノート）のような質感」でパラパラとページをめくって閲覧できる特別なブックビューアUIを提供します。
    * ポスト内の交換日記詳細画面に「**次の人へ書いて送信**」ボタンを配置し、タップするとシームレスに手紙作成と同じフロー（全画面エディタ）へと遷移します。
    * 「秘密の話」や「グッドニュース」「バッドニュース」「質問相談コーナー」「恋バナ」など、一周回るごとに話題のテーマ（お題）がシステムから与えられます。
* **【新規】交換日記の執筆滞留（ストップ）対策（UX維持仕様）**:
    * リレー形式の交換日記が特定のユーザーで止まるのを防ぐため、自分の番になってから**48時間**以上執筆がない場合、システムが該当ユーザーへ自動で催促通知（FCM）を送信します。
    * **72時間**が経過した場合、日記のステータスを一時的に `overdue`（期限切れ）に変更し、グループの管理者（オーナー）または他のメンバーの画面（フレンドタブ内のグループ管理画面）に「現在の執筆者をスキップして次の人へ日記を強制転送する」ボタンを表示します。これにより、グループ全体の機能停止を防止します。

#### ② レーダー（置き手紙レーダー）
* フレンドまたは所属グループのメンバーが、特定のGPS位置に設置した「置き手紙（Drop Letter）」を探すための機能です。距離（50m以内）と方向に基づいてレーダー風の画面上にプロットし、実際にその場所まで近づくことで発見・開封できます。

#### ③ 手紙作成 / 交換日記執筆（イマーシブ・全画面エディタ）の詳細UI仕様
下部ナビゲーション中央の丸く大きな「手紙作成」ボタン、またはポストの「次の人へ書いて送信」ボタンを押した瞬間から、手紙がポストへ投函されるまでの詳細なUI/UX遷移を以下に定義します。

1. **没入型（イマーシブ）画面遷移**:
    * ボタンがタップされた瞬間、**下部ナビゲーションバーが画面下方へスムーズにスライドアウトして完全消失**します。
    * 同時に、画面全体が執筆専用のエディタ背景へとシームレスに切り替わり、ユーザーが手紙の執筆に100%没頭できるフルスクリーン空間へと誘います。

2. **「かく（紙選び）」のUI詳細**:
    * 最初に便箋（かみえらび）画面が起動します。画面中央にレターセットの束がリアルな影を伴う質感で配置されます。
    * 左右へのスワイプジェスチャーにより、本物の紙を「ベラベラ、パラパラ」とめくって選ぶような、心地よいスクロールエフェクトと微細な紙の摩擦音（SE）が走ります。
    * 各便箋には、温かみのあるクラフト紙、淡いサクラ色、夜空を模した濃紺など情緒的なテーマが割り当てられており、お気に入りを選んで下部の「この便箋にする」をタップして決定します。

3. **「かき中（エディタ・部分スタイル適用）」のUI詳細**:
    * **画面構成**: 決定した便箋テーマ（罫線や地紋など）が画面全面の背景に敷かれ、その上にスクロール可能なテキスト入力エリアが広がります。
    * **【新規】Firestore 1MB制限に伴うソフトリミット**: データ容量の逼迫やメタデータ肥大化によるバグを防ぐため、アプリのエディタ側で「最大30,000文字まで」、および「スタイル変更（リッチテキストの区切れ）は最大200箇所まで」という実用上十分なソフト制限（バリデーション）を実装します。上限に達しそうな場合は「少し手紙が長すぎるようです。新しいページに分けましょう」等の案内を表示します。
    * **上部固定ツールバー**: 以下の3つのトグルボタンが配置されます。
        * *ペン（フォント選択）*: 「既定のゴシック」「既定の明朝」「手書き風」「自分のオリジナルフォント」を切り替えます。
        * *色（カラーパレット）*: 墨、セピア、藍色、茜色といった、便箋に馴染む落ち着いた色のインクを選択できます。
        * *文字サイズ*: 小・中・大・特大を切り替えるステップトグル。
    * **部分スタイル（個別フォント）適用機能**: テキスト入力中、任意の文字列を長押ししてドラッグ選択すると、選択範囲のすぐ上にフローティングポップアップメニューが出現します。ここから特定の文字や単語、1文章のブロック単位に対してのみ、フォント・色・サイズを個別に変更できます。「手紙の基本は明朝体で書き、感情を高ぶらせたい一言だけを自分のオリジナルフォント（手書き）と大きな赤い文字に変える」といったリッチな表現が可能です。
    * **軽量な文字データ通信（画像化の禁止）**: 通信コストおよびデータベース容量を最小限に抑えるため、手紙データはすべて純粋な「文字（テキスト）データ」および「スタイル・位置のメタデータJSON」として送信します。画面の画像化（ラスタライズデータの送信）は行いません。
    * **画像添付スロット**: 便箋の最下部（または改ページ直前）に固定の「＋」スロットが1箇所配置されます。タップするとカメラまたはギャラリーが起動します。写真はポラロイドカメラ風の白枠フレームに自動加工され、便箋の上に**1枚のみ**美しくレイアウトされます。
    * **改ページUI**: 「＋ページを追加」ボタンをタップすると、現在の便箋の下に新しいページがスライドインで追加されます。ページごとに異なる便箋デザインを「紙選び」から再選択することが可能です。

4. **「送信前の不足文字その場作成ステップ」のUI詳細**:
    * ユーザーが執筆を終えて「かきおわった！」ボタンをタップすると、システムが本文テキストを走査し、ユーザーが`まだオリジナルフォントに登録していない未作成の文字`が含まれているかどうかを瞬時に検出します。
    * 未登録の文字が検出された場合、自動的に全画面のステップ画面が起動し、「手紙を相手に届けるために、あと○文字だけあなたの手を貸してください」というメッセージが表示されます。
    * **文字作成のUI構成**:
        * *上部（お手本表示）*: システムフォントで「次に書く対象の文字（例：『鬱』『☆』）」が1文字、画面中央上部に大きくプレビューされます。
        * *中央（手書きキャンバス）*: 画面の横幅いっぱいに、**「大きな正方形（1:1）の手書きキャンバスエリア」**が配置されます。キャンバス内には、文字のバランスを取りやすくするための薄い点線の十字ガイド（十文字の補助線）が敷かれています。ユーザーはこの四角形の中に直接文字を肉筆で書き込みます。
        * *下部（コントロール）*: 「書き直す（クリア）」ボタンと「できた！」ボタンが配置されます。
        * **【新規】ストローク数バリデーションと平滑化（スムージング）**: キャンバスの描画データ（パス）が「0ストローク（白紙）」の場合は「できた！」ボタンを非活性化します。また、あまりにも複雑な塗りつぶしの場合はデータ肥大化防止のため「もう少しシンプルに書いてください」と制限をかけます。指で書いた線のガタつきを綺麗に見せるため、Flutter側でサンプリング点の補間（ベジエ曲線化・平滑化フィルター）を施してからFirestoreへベクター保存し、おしゃれな質感とデータ軽量化を両立します。
    * すべての不足文字の書き込みが完了すると、自動的にフォントのバージョンがインクリメントされ、Firestoreへベクターデータ（SVG形式）がアップロードされた後、次の封緘（ふうかん）フェーズへ自動で遷移します。

5. **「かきおわり・しまう演出（封緘）」のUI詳細**:
    * カルーセル画面から手紙を包む「ふうとう（封筒）」と、留める「シール」を選択します。
    * 「封をする」をタップすると、それまで広がっていた便箋がアニメーションによって美しく三つ折りに折りたたまれていき、選んだ封筒の口へ「シュッ」と吸い込まれるように収まります。その後、封筒の蓋が閉じ、選んだシールが「ポン」と小気味よいエフェクトとともに貼り付けられ、「てがみ かんせい！」状態の封筒が画面中央に直立するリアルなフルアニメーションが実行されます。

6. **「おくる（配送選択・グループ1人限定受信オプション）」のUI詳細**:
    * 完成した封筒の下部に、配送手段を選択する大型カードボタン（郵送：レトロカー、速達：鳥、置き手紙：方位磁針）が並びます。
    * 物理的距離から算出された所要時間が、手書き風フォントの吹き出しで**「約3時間後」「約2日後にお届け」**とおおまかな目安時間としてポップアップ表示されます。
    * **【新規】グループ置き手紙における「1人だけが受信できる機能」の選択**:
      * グループ宛てに「置き手紙（Dropモード）」を設置する際、以下のオプションを選択可能にします。
        1. **全員受信モード**: グループメンバー全員がそれぞれのタイミングで回収できるモード。
        2. **先着1人限定（早い者勝ち）モード**: グループメンバーのうち、誰か最初の1人がその場所に近づいて手紙を発見・回収した時点で、他のメンバーのレーダーからは消失し、回収できなくなるモード。
        3. **グループ内特定の1人指定モード**: グループという文脈・繋がりを維持したまま、グループ内の特定のメンバー1人だけを指定してその場所に設置するモード（指定された本人しかレーダーに表示されず、回収も不可）。
    * 「この方法でポストに入れる」ボタンをタップすると、画面いっぱいに描かれたレトロなポストの投函口へ封筒が滑り落ちていく送信完了アニメーションが流れます。
    * 送信完了後、**隠されていた下部ナビゲーションバーがスライドインで元の位置に復活**し、自動的に「ポスト」タブへと画面が引き戻されます。

#### ④ フレンド（フレンド・グループ管理）
従来のフレンド一覧と、新設されるグループ管理機能を統合したコミュニケーションハブ画面です。
* **フレンド管理**: 「フレンドID検索」「QRコードスキャン」「近距離通信（BLE等を用いた近接端末マッチング）」によるフレンド申請・承認・一覧表示。
* **グループ作成（招待制）**: 
    * 任意のフレンドを複数人選択して、招待制の「グループ」を作成できます。
    * グループ宛てには「メンバー全員への一斉送信手紙」や、上述した「グループメンバーなら誰でも開けられる置き手紙 / 1人限定置き手紙」の作成・設置が可能になります。
* **交換日記の同時生成機能**:
    * グループ作成時に「**交換日記も作成する**」チェックボタンを提供します。
    * これを有効にすると、グループ作成と同時に以下の設定画面へ遷移します。
        * **交換順番の設定**: メンバーの一覧をドラッグ＆ドロップで並び替え、日記を回すローテーション順序（例: A → B → C → A...）を決定します。
        * **交換日記のデザイン選択**: 日記の表紙やベースとなるノートのテーマデザインを選択します。
    * 設定完了後、最初の執筆者が「手紙作成」と同様の流れで第1ページ目を執筆し、送信することで交換日記リレーがスタートします。

#### ⑤ マイページ（旧「設定」統合型プロフィール）
* **アカウント・プロフィール編集**: 自身の表示名、アイコン画像、およびフレンドコード（3〜15文字以上、重複不可バリデーションあり）の管理。
* **手書きオリジナルフォント作成機能（よく使う順作成UI）**:
    * ユーザーはマイページからいつでもオリジナルフォントの作成・追加登録を行うことができます。
    * **作成順序の最適化**: ユーザーが効率的にフォントを成長させられるよう、提示文字は利用頻度の高いカテゴリ順（**ひらがな → よく使う記号 → カタカナ → アルファベット → 常用漢字 → その他**）で1文字ずつステップアップしていく構造とします。
    * **フォント作成UI**: 手紙作成時の不足文字作成ステップと同様のインターフェースを踏襲し、画面中央に「次に書く対象の文字（お手本）」を1文字大きく表示、その下部に配置された**「大きな正方形（四角形）の手書きキャンバス入力欄（点線十字ガイド付き）」**の中に直接文字を肉筆で書き込むスタイルとします。
* **アプリ設定項目**: カラーモード（ライト/ダーク/システム同期）、多言語切り替え（日本語/英語など）の設定項目をこの画面内に集約します。

---

## 2. Firebase アーキテクチャ設計＆セキュリティ・コスト最適化設計

### 2.1 Cloud Firestore (データベース設計)


```

/users/{uid} (Document)
├── displayName: String
├── friendId: String
├── photoUrl: String?
├── lastLocation: GeoPoint
├── settings: Map (theme: String, language: String)
├── weeklyExpressCount: Integer
├── lastExpressResetAt: Timestamp
├── fontSettings: Map (hasCustomFont: Boolean, currentFontVersion: Integer)
└── createdAt: Timestamp

/groups/{groupId} (Document)
├── name: String
├── ownerId: String
├── memberIds: Array [uid1, uid2, ...]
├── hasActiveDiary: Boolean
├── diaryId: String?
└── createdAt: Timestamp

/friend_requests/{requestId} (Document)
├── senderId: String
├── recipientId: String
├── status: String (pending, accepted, rejected)
└── createdAt: Timestamp

/friends/{friendshipId} (Document)
├── userIds: Array [uid1, uid2]
└── establishedAt: Timestamp

/letters/{letterId} (Document)
├── senderId: String
├── senderName: String
├── recipientId: String?
├── recipientGroupId: String?
├── isGroupDrop: Boolean // グループ宛ての置き手紙か
├── isSingleRecipientDrop: Boolean // 【新規】グループ置き手紙で1人だけが受信可能（限定）か
├── singleRecipientMode: String? // 【新規】"first_come" (先着1人) または "designated" (特定1人指定)
├── designatedRecipientId: String? // 【新規】特定1人指定の場合の対象ユーザーUID
├── openedUserIds: Array [uid1, uid2, ...] // 開封したユーザーのUIDリスト
├── discoveredUserIds: Array [uid1, uid2, ...] // 【新規】レーダーで見つけて受け取り（ダウンロード）済みのユーザーIDリスト
├── mode: String (express, normal, drop)
├── pages: Array [
{
content: String,
paperTheme: String,
textStyles: Array [
{ start: Integer, end: Integer, fontType: String, fontVersion: Integer?, fontSize: Double, color: String }
] // 文字・ブロック単位の部分フォント・スタイル適用メタデータ
}
]
├── photoUrl: String?
├── envelopeTheme: String
├── sealTheme: String
├── sentAt: Timestamp
├── estimatedDeliveryAt: Timestamp
├── geoLatitude: Double?
├── geoLongitude: Double?
├── status: String (transit, delivered) // 個人の場合はこれで管理。グループの場合はdiscoveredUserIds等で個別管理
└── deliveryDurationSeconds: Integer

/diaries/{diaryId} (Document)
├── groupId: String
├── title: String
├── designTheme: String
├── rotationOrder: Array [uid1, uid2, uid3, ...]
├── currentTurnIndex: Integer
├── currentTurnUserId: String
├── status: String (reading, writing, transit, overdue) // 【新規】overdue（滞留期限切れ）を追加
├── estimatedDeliveryAt: Timestamp?
└── updatedAt: Timestamp

/diaries/{diaryId}/entries/{entryId} (Subcollection)
├── writerId: String
├── writerName: String
├── pages: Array [
{
content: String,
diaryPageTheme: String,
textStyles: Array [ { start: Integer, end: Integer, fontType: String, fontVersion: Integer?, fontSize: Double, color: String } ]
}
]
├── photoUrl: String?
├── sentAt: Timestamp
└── sequenceNumber: Integer

/custom_fonts/{uid} (Document)
├── currentVersion: Integer
├── fontVersions: Map { "1": "https://...", "2": "https://..." } // 必要なデータのみを格納した軽量ファイルのURL
└── updatedAt: Timestamp

/custom_fonts/{uid}/glyphs/{character} (Subcollection)
├── character: String
├── vectorData: String // 手書きのベクターパスデータ（SVG形式）
├── versionAdded: Integer
└── updatedAt: Timestamp

```

### 2.2 フォントデータのストレージ＆通信量最適化設計
フォントデータによる通信量とサーバーコストを徹底的に削減するため、以下の「手紙専用の最小データ転送」仕様を確定適用します。

* **サブセットフォント/ストローク差分同期の確定採用**:
  * ユーザーのオリジナルフォントを読み込む際、数MBに及ぶ日本語の全文字入りTTF/OTFファイルを丸ごと転送することは一切禁止します。
  * **実装アプローチ**: サーバー（Firestore）上には文字単位の軽量ベクターデータ（`vectorData`: 数百バイトのSVG形式）のみを保管。手紙送信時に、アプリ側またはFunctionsで**「その手紙に実際に使用されている文字データのみ」を抽出・パッケージングした極小のサブセットファイル**、あるいは文字対応のストロークJSONデータのみを生成して転送します。
  * これにより、フォント関連の通信・ファイルサイズは手紙1通あたりわずか数KB〜数十KBへと極小化されます。

### 2.3 セキュリティ・悪用防止・コスト最適化設計
Firebaseの不正利用を防止し、インフラコストが跳ね上がらないよう、以下の設計ルールをセキュリティルールおよびクライアントロジックに強制適用します。

#### ① Firestore セキュリティルール（Security Rules）の徹底
* **Userコレクション**: `request.auth.uid == uid` の場合のみ書き込み可能（他人のプロフィールの改ざん禁止）。
* **Lettersコレクション**: 
  * `create`: 送信者（`senderId`）が自分自身であること、かつ1回のリクエストサイズや週次速達制限回数が正しいことを検証。
  * `read`: `request.auth.uid == resource.data.senderId` または `request.auth.uid == resource.data.recipientId` または `request.auth.uid in get(/databases/$(database)/documents/groups/$(resource.data.recipientGroupId)).data.memberIds` の場合のみ許可（無関係な第三者による手紙の盗聴を100%遮断）。
  * **【新規】1人限定グループ置き手紙の制御**: `isSingleRecipientDrop == true` かつ `singleRecipientMode == "designated"` の場合、`request.auth.uid == resource.data.designatedRecipientId` のユーザー、または送信者のみがドキュメントを読めるように制限します。

#### ② Firebase Storage セキュリティルールと容量制限
* **画像アップロード制限**:
  * パスを `/letters/{letterId}/attached_photo.jpg` または `/diaries/{diaryId}/{entryId}.jpg` に厳密に固定。
  * ルール側で `request.resource.size < 5 * 1024 * 1024`（最大5MB以下）および `request.resource.contentType.matches('image/.*')`（画像ファイルのみ）のバリデーションを強制し、大容量ファイルや無関係なスクリプトファイルのアップロードによるストレージコスト逼迫を防止。

#### ③ Firebase App Check の導入
* デバイスの整合性（iOS: DeviceCheck/App Attest, Android: Play Integrity）を検証する **Firebase App Check** を有効化。Curlコマンドや外部スクリプト、偽装されたAPIリクエストによるFirestore/Storageへの不正アクセスや悪意ある大量リクエスト（リソース浪費）をSDKレイヤーで完全にブロックします。

#### ④ コスト削減のためのクエリ・キャッシュ戦略
* クライアント側（Flutter）でFirestoreの **Local Cache（永続性ディスクキャッシュ）** を有効化。ポスト内の過去の手紙や交換日記の一覧、フレンド一覧などは、一度取得したデータはキャッシュから読み込み、ドキュメントの変更差分（`snapshots()`）のみをリッスンすることで、FirestoreのRead（読み込み）リクエスト回数とコストを劇的に削減します。
* レーダー機能（置き手紙検索）においては、全件取得を絶対に避け、Geohash（近傍検索クエリ）を用いて自身の現在地周辺（50m~数km圏内）のドキュメントのみを限定的にフックする設計とします。
* **【新規】グループ置き手紙の出現クエリの最適化**: 置き手紙の出現クエリは「自分が `discoveredUserIds` に含まれていない、かつグループ宛ての `drop` モードの手紙」として取得します。さらに先着1人限定モード（`first_come`）の場合は、「既に `discoveredUserIds` の配列サイズが0であるもの」という条件を加え、誰か1人が拾った時点でクエリの対象外となるようクライアント側でフィルタリングします。

### 2.4 【新規】バックグラウンド処理・到着判定と通知の自動化
* **Google Cloud Tasks の連携アーキテクチャ**:
  * 手紙や交換日記が送信された際、Cloud Functionsが起動し、そのアイテムの `estimatedDeliveryAt`（到着予定時刻）に実行されるタスクを **Google Cloud Tasks** にスケジュール登録（キューイング）します。
  * 指定時刻（到着時間）になったら、Cloud Tasksが通知用Functionをキックし、手紙のステータスを `transit` から変更（個人宛ての場合）、および宛先ユーザーへFCMプッシュ通知を正確に送信します。これにより、アプリが閉じているバックグラウンド状態でも「スローライフ」な到着の瞬間を逃さずユーザーに通知できます。

---

## 3. 開発・移行タスク一覧（TODOリスト）

### フェーズ 1 ~ 2: 基盤構築・アカウント・フレンド
- [ ] Firebaseプロジェクト初期化、匿名認証の実装
- [ ] 近距離通信(BLE)・QR・ID検索によるフレンド追加機能の実装
- [ ] **Firebase App Check** の導入とiOS/Androidのキー/プロバイダ設定（悪用防止策）

### フェーズ 3: ナビゲーション再構築と全画面エディタの実装
- [ ] **ナビゲーション構造の刷新**: `ポスト` / `レーダー` / `手紙作成` / `フレンド` / `マイページ` の5タブ構成（主にFlutterのNavigator 2.0 / GoRouter等を利用）
- [ ] **全画面イマーシブUI**: 手紙作成ボタン押下時にナビゲーションバーをスムーズに下方非表示化する処理の実装
- [ ] **文字データエディタと部分フォント適用ロジックの実装**:
  - [ ] 画像化せず、純粋なテキスト＋スタイルメタデータ（`textStyles`）でFirestoreに保存する仕組みの構築
  - [ ] 30,000文字 / スタイル200箇所の上限バリデーション（ソフトリミット）をFlutter側エディタに実装
  - [ ] 文字・ブロック単位でオリジナルフォントと既定フォントを混在して適用できるエディタUIおよび長押し選択ポップアップの開発
  - [ ] 添付画像を1枚のみに制限し、5MB以下に圧縮してStorageにアップロードするロジックの実装

### フェーズ 4 ~ 5: グループ・交換日記生成および拡張機能の実装
- [ ] **フレンドタブの構築**: フレンド一覧、招待制グループ作成UIの実装
- [ ] **交換日記同時生成処理**: ドラッグ＆ドロップによる順番設定、テーマ選択、および初回執筆フロー連携
- [ ] **交換日記の滞留（ストップ）対策ロジックの実装**:
  - [ ] 48時間以上の未執筆で催促FCMを送信するCloud Functionsのタイマー処理実装
  - [ ] 72時間以上経過で `status: overdue` に変更し、グループ画面に強制転送スキップボタンをFlutter側で表示する処理の実装
- [ ] **グループ拡張機能（一斉送信・置き手紙拡張）**:
  - [ ] `recipientGroupId` を用いたメンバー全員への一斉送信の実装
  - [ ] グループ置き手紙において「全員受信」「先着1人限定（早い者勝ち）」「特定1人指定」の3つのオプション選択UIおよび、Firestore保存ロジックの実装
  - [ ] レーダー画面における `discoveredUserIds` を用いた個別出現・消失クエリ（Geohash連携）の実装

### フェーズ 6: 交換日記リレーシステムと「不足文字補完」の実装
- [ ] **送信前不足文字検出＆その場作成ステップの開発**:
  - [ ] 手紙作成完了時（封緘前）、本文の文字をスキャンし、`custom_fonts/{uid}/glyphs` に存在しない文字を抽出するロジックの実装
  - [ ] 未登録文字がある場合、全画面ステップUIを起動し、中央に「お手本文字」を提示、「点線十字ガイド付きの大きな正方形手書きキャンバス（1:1）」で肉筆入力させるFlutterインターフェースの構築
  - [ ] 0ストロークチェックのバリデーション、および入力されたパスのベジエ曲線スムージング（平滑化フィルタ）処理をFlutter（CustomPainter / Path等）で実装
- [ ] **本・ノート風ビューア**: ポストから交換日記を開いた際、過去エントリを本のようにスワイプしてめくれるUIの実装
- [ ] **リレー連携**: 「次の人へ書いて送信」ボタンから全画面エディタを起動し、ターンインデックスを更新して次のメンバーへ配送するロジックの実装

### フェーズ 7: マイページオリジナルフォント作成機能の高度化
- [ ] **よく使う順作成ステップUIの実装**:
  - [ ] マイページのフォント作成画面において、ひらがな → 記号 → カタカナ → アルファベット → 常用漢字 → そのその他の順序で文字を提示する制御ロジックの実装
  - [ ] 「大きな四角形（1:1）の手書きキャンバス」を中心とした、洗練されたフォントライティングUIの構築

### フェーズ 8: セキュリティ強化・コスト最適化・バックエンド構築・テスト
- [ ] **Firestore / Storage Security Rules の厳格な記述とデプロイ**（他人の手紙閲覧禁止、グループ置き手紙の閲覧権限、容量制限等）
- [ ] **Google Cloud Tasks 連携によるバックグラウンド到着判定＆通知システム**の実装
- [ ] クエリのローカルキャッシュ有効化設定と読み込み数抑制のテスト
- [ ] Firebase Emulator Suiteを用いた、セキュリティルールのバリデーションテスト、および交換日記リレーの統合結合テスト